# Greengenes

This reference was originally intended to be used with the alternate
classification script written in the `lca` directory. It's currently not
being used (but it could be if you really wanted to use it).
